# Farmacia - Turnos (UI hardcodeada)

Objetivo: validar pantallas con el cliente con datos hardcodeados.
Luego: meter lógica de negocio + SQLite.

## Ejecutar (Windows)
```powershell
py -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r .\requirements.txt
python -m farmacia_app.main
